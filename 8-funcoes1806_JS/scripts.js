
let a = 2;
let b = 3;


function somar(a, b) {
    let sum = (a + b);
    return sum;
}

function subtrair(a, b) {
    let sub = (a - b);
    return sub;
}

function multiplicar(a, b) {
    let multi = (a * b);
    return multi;
}

function dividir(a, b) {
    let divi = (a/b);
    return divi;
}

// somar(a, b);
// subtrair(a, b);
// multiplicar(a, b);
// dividir(a, b);

console.log("Somar:" + somar(a,b), "Subtrair:" + subtrair(a, b), "Multiplicar:" + multiplicar(a, b), "Dividir:" + dividir(a, b));

